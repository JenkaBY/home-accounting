<% @index = true %>

<%= render 'finances/filter' %>

<table class="table table-striped table-sm table-condensed">
  <thead class="head">
  <tr>
    <th><%= t('dat') %></th>
    <th><%= t('amount') %></th>
    <th><%= t('category') %> <%= t('and') %> <%= t('description') %></th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <%= render @finance %>
</table>

<%= render 'finances/new' %>